import { formJson } from './helper-data'

const restHelper = {

  // the fetches were small so I put them streigth in the actions (formConfig.js),
  // but this helper is at ready for use

}

export default restHelper;